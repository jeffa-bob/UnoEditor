<UserControl
    x:Class="UnoEditor.Shared.Views.Editor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoEditor.Shared.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

  <TextBox Text="{x:Bind fulltext}"></TextBox>
  <!--<ListView ItemsSource="{x:Bind filetextlines}">
    <ListView.ItemTemplate>
      <DataTemplate x:Name="filelines" x:DataType="x:String">
        <TextBlock Text="{x:Bind}" HorizontalAlignment="Stretch"/>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>-->
  
</UserControl>
